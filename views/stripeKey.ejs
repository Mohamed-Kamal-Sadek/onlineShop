<!DOCTYPE html>
<html>
    <title>Stripe Payment Demo</title>
    <body>
        <h3>Welcome to Payment Gateway</h3>
        <form action="/api/stripe/payment/<%= cartId %>" method="POST">
            <script
                src="//checkout.stripe.com/v2/checkout.js"
                class="stripe-button"
                data-key="<%= key %>"
                data-amount="<%= cartPrice %>" 
                data-currency="usd"
                data-name="Sadek Online Shop"
                data-description="T-Shirts & Shoes & Perfumes"
                data-locale="auto" >
            </script>
            <input type="hidden" name="data_amount" value="<%= cartPrice %>">
        </form>
    </body>
</html>